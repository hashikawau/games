<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.games.tetris">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screen.TetrisMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.games.TETRIS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".screen.TetrisActivity"
            android:label="@string/tetris"
            android:parentActivityName=".screen.TetrisMenuActivity"></activity>
    </application>

</manifest>