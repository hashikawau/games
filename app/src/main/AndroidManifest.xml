<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.games">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".tetris.screen.TetrisMenuActivity"
            android:label="@string/tetris"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name=".tetris.screen.TetrisActivity"
            android:label="@string/tetris"
            android:parentActivityName=".tetris.screen.TetrisMenuActivity">
        </activity>
        <activity
            android:name=".breakblocks.screen.BreakBlocksActivity"
            android:label="@string/tetris"
            android:parentActivityName=".tetris.screen.TetrisMenuActivity">
        </activity>
    </application>

</manifest>